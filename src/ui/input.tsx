/// <reference types="ink" />

/**
 * Input panel component — handles user text input with history.
 */

import React from 'react';
import { Text } from 'ink';

export const InputPanel: React.FC = () => {
  return React.createElement(Text, { dimColor: true }, '[Input panel — Phase 1E]');
};
